// Fonction that will print a message in the console
const displayMessage = (message) => {
    // Use console.log to print the message in STDOUT
    console.log(message);
};

// Export the displayMessage function to use it in other files
module.exports = displayMessage
